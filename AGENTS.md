- 単に動作するだけのコードではなく、将来的な可読性や保守性、セキュリティを重視して実装する
- 行数を増やして安全マージンを稼ぐよりもシンプルかつロバストに実装する
- 実装コンポーネントごとの責務に従ってコードを書く
- DRY原則を守る
- すべての .ts / .tsx ファイルやクラス、関数、インタフェースの冒頭に、その役割や責務を1-3行程度で書く
- シンプルなコード以外はTSDocを書く
- 冗長なテストコードを避け、実装詳細ではなく振る舞いをテストすることで将来的な実装側の変更に追従しやすいテストコードを書く
- 使用されていないコードは積極的に削除する
- DBを使う実装では、フルスキャン / USE TEMP B-TREE にならないことをインデックス、クエリから確認し、想定クエリをEXPLAINするなど確認してから実装する
- ドキュメントはシンプルかつ的確に、読み手が欲しい情報を見つけられるように構造化して書く
- レビューコメントは建設的な改善提案として受け取る
